{
  "name": "tidytabs",
  "version": "1.0.29",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist && mkdir -p dist",
    "format": "prettier --write .",
    "lint": "eslint . --ext .js",
    "zip": "VERSION=$(grep '\"version\"' manifest.json | cut -d'\"' -f4) && zip -r dist/tidytabs-$VERSION.zip manifest.json icons/icon16.png icons/icon48.png icons/icon128.png popup options src -x \"*/.*\"",
    "build": "npm run clean && npm run format && npm run zip"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.2.5"
  }
}
